loat a, b, c;
int d=5, e=4;
void setup()
{
  Serial.begin(9600);
  a = 3;
  b = 2;
  
  c=a+b;
  Serial.println(c);
  
  c=a-b;
  Serial.println(c);
  
  c=a*b;
  Serial.println(c);
  
  c=a/b;
  Serial.println(c);
    
  c=d%e;
  Serial.println(c);
}

void loop()
{
}
